<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="<%= asset_path('styles.css') %>">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    
    <title>Driving School</title>
    <style>
    #about-container, #service-container, #contact-container {
      max-width: 100%;
      margin: 0 auto;
  }
  
    body {
  max-width: 100vw; /* Ensures it doesn't exceed viewport width */
  overflow-x: hidden; /* Prevents horizontal scrolling */
}
</style>
</head>
<body>
  <div>
<header class="heading">
    <div class="container text-center">
        <h3>Bumper Finance Available on Intensive Courses</h3>
    </div>
</header>

<!-- Banner Section -->
<div class="banner" style="background-image: url('<%= asset_path("home_background.png") %>');">
    <div class="text-content">
        <h1 style="font-size: 3.5em;">Building <strong>Confident Drivers</strong>,<br> One Lesson at a Time.</h1>
        <p style="font-size: larger;">Whether you’re a beginner or seeking to refine your skills, our<br>experienced instructors provide personalized lessons designed to<br>build confidence, ensure safety, and prepare you for the road<br>ahead. Join us today and take the first step toward independence!</p>
        <br><br><a href="/book_session" class="btn-custom">Book Session Now</a>
    </div>
</div>    </div>
    <%= render 'suggestion' %>
  <div id="about-container">
    <%= render 'about' %> <!-- Render About content initially -->
  </div>
  <div id="service-container">
    <div style="background-color: #7C46FD; margin-top: 12%;"> 
      <%= render 'service' %>
    </div>
    <%= render 'experience' %>
  </div>
  <div id="contact-container">
    <%= render 'contact_us' %>
  </div>

<script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

</body>
</html>
<script>
document.addEventListener("DOMContentLoaded", function() {
  document.querySelectorAll("nav a").forEach(link => {
    link.addEventListener("click", function(event) {
      let targetSection = this.id?.replace("-link", "-container");
      let targetElement = document.getElementById(targetSection);

      // Only prevent default behavior for section links
      if (targetElement) {
        event.preventDefault();
        window.scrollTo({
          top: targetElement.offsetTop - 50,
          behavior: "smooth"
        });
      }
    });
  });
});

</script>
